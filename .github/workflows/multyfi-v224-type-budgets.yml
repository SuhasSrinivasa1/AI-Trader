name: Build Multyfi AutoBuy Pro v2.2.4 Trade Type Budgets

on:
  pull_request:
    branches: [main]
    paths:
      - 'hotfix/run_v224.py'
      - 'hotfix/run_v224_safe.py'
      - '.github/workflows/multyfi-v224-type-budgets.yml'
  workflow_dispatch:

jobs:
  build-test-android16:
    runs-on: ubuntu-latest
    timeout-minutes: 45
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '17'

      - uses: android-actions/setup-android@v3

      - name: Install Android 16 SDK
        run: yes | sdkmanager "platforms;android-36" "build-tools;35.0.0" "platform-tools" "emulator" "system-images;android-36;google_apis;x86_64"

      - uses: gradle/actions/setup-gradle@v4
        with:
          gradle-version: '8.13'

      - name: Apply v2.2.4 source update
        shell: bash
        run: |
          set +e
          python3 hotfix/run_v224_safe.py > apply-v224.log 2>&1
          code=$?
          cat apply-v224.log
          echo "$code" > apply-v224-exit-code.txt
          exit "$code"

      - name: Unit tests, lint and source-built APK
        shell: bash
        run: |
          set +e
          gradle --no-daemon --console=plain -p android-stable :app:testDebugUnitTest :app:lintDebug :app:assembleDebug > build-v224.log 2>&1
          code=$?
          cat build-v224.log
          echo "$code" > build-v224-exit-code.txt
          exit "$code"

      - name: Verify trade-type budgets and retained safety contracts
        shell: bash
        run: |
          set -euo pipefail
          ROOT=android-stable/app/src/main/java/com/suhas/multyfiautobuy/stable
          TEST=android-stable/app/src/test/java/com/suhas/multyfiautobuy/stable
          grep -q "versionCode 224" android-stable/app/build.gradle
          grep -q "versionName '2.2.4'" android-stable/app/build.gradle
          grep -q "class TradeTypeBudgetPolicy" "$ROOT/TradeTypeBudgetPolicy.java"
          grep -q 'return AppPrefs.intradayBudget(context)' "$ROOT/TradeTypeBudgetPolicy.java"
          grep -q 'return AppPrefs.swingBudget(context)' "$ROOT/TradeTypeBudgetPolicy.java"
          grep -q 'return AppPrefs.multibaggerBudget(context)' "$ROOT/TradeTypeBudgetPolicy.java"
          grep -q 'return AppPrefs.freeBudget(context)' "$ROOT/TradeTypeBudgetPolicy.java"
          grep -q "setTradeTypeBudgets" "$ROOT/AppPrefs.java"
          grep -q "TradeTypeBudgetPolicy.budget" "$ROOT/ProductionNotificationService.java"
          grep -q "budget type" "$ROOT/ProductionNotificationService.java"
          ! grep -q "OrderPolicy.activeBudget" "$ROOT/ProductionNotificationService.java"
          grep -q "Intraday maximum budget" "$ROOT/ProductionActivity.java"
          grep -q "Swing / ordinary equity budget" "$ROOT/ProductionActivity.java"
          grep -q "Multibagger budget" "$ROOT/ProductionActivity.java"
          grep -q "Free recommendation budget" "$ROOT/ProductionActivity.java"
          grep -q "Free recommendations use the saved Free budget" "$ROOT/ProductionActivity.java"
          ! grep -q "exactly 10 shares" "$ROOT/ProductionActivity.java"
          ! grep -q "FREE fixed quantity 10" "$ROOT/ProductionActivity.java"
          grep -q "recommendationTypeSelectsBudgetNotClock" "$TEST/TradeTypeBudgetPolicyTest.java"
          grep -q "createMisStopLossOrder" "$ROOT/GrowwClient.java"
          grep -q 'body.put("product", "MIS")' "$ROOT/GrowwClient.java"
          grep -q 'body.put("order_type", "SL_M")' "$ROOT/GrowwClient.java"
          grep -q "ProtectionOrderPolicy.usesRegularMisStop" "$ROOT/StrategyMonitorService.java"
          grep -q "DailyAuthManager.ensureVerified(this)" "$ROOT/ProductionNotificationService.java"
          grep -q "NotificationRoutePolicy.entryMode(signal)" "$ROOT/ProductionNotificationService.java"
          grep -q "0.10d" "$ROOT/GrowwClient.java"

      - name: Verify APK signature and stage candidate
        shell: bash
        run: |
          set -euo pipefail
          OUT=android-stable/app/build/outputs/apk/debug
          $ANDROID_HOME/build-tools/35.0.0/apksigner verify --verbose --print-certs "$OUT/app-debug.apk"
          cp "$OUT/app-debug.apk" "$OUT/Multyfi-AutoBuy-Pro-S24-Android16-v2.2.4-CANDIDATE.apk"

      - name: Enable KVM access
        run: |
          echo 'KERNEL=="kvm", GROUP="kvm", MODE="0666", OPTIONS+="static_node=kvm"' | sudo tee /etc/udev/rules.d/99-kvm4all.rules
          sudo udevadm control --reload-rules
          sudo udevadm trigger --name-match=kvm

      - name: Android 16 install, two cold launches and crash scan
        timeout-minutes: 20
        shell: bash
        run: |
          set -euo pipefail
          OUT=android-stable/app/build/outputs/apk/debug
          export ANDROID_AVD_HOME="$RUNNER_TEMP/android-avd"
          export ANDROID_SDK_HOME="$RUNNER_TEMP/android-sdk-home"
          mkdir -p "$ANDROID_AVD_HOME" "$ANDROID_SDK_HOME/.android"
          AVD_PATH="$ANDROID_AVD_HOME/s24_v224_api36.avd"
          echo no | avdmanager create avd --force --name s24_v224_api36 --package 'system-images;android-36;google_apis;x86_64' --device 'pixel_8_pro' --path "$AVD_PATH"
          "$ANDROID_HOME/emulator/emulator" -avd s24_v224_api36 -no-window -gpu swiftshader_indirect -no-snapshot -noaudio -no-boot-anim -camera-back none > "$OUT/emulator-v224.log" 2>&1 &
          EMULATOR_PID=$!
          trap 'timeout 15 adb emu kill >/dev/null 2>&1 || true; kill "$EMULATOR_PID" >/dev/null 2>&1 || true' EXIT
          timeout 240 adb wait-for-device
          booted=0
          for i in $(seq 1 100); do
            state=$(timeout 10 adb shell getprop sys.boot_completed 2>/dev/null | tr -d '\r')
            if [ "$state" = "1" ]; then booted=1; break; fi
            if ! kill -0 "$EMULATOR_PID" 2>/dev/null; then break; fi
            sleep 4
          done
          test "$booted" = "1"
          APK="$OUT/Multyfi-AutoBuy-Pro-S24-Android16-v2.2.4-CANDIDATE.apk"
          adb install -r "$APK" | tee "$OUT/install-v224.txt"
          grep -q Success "$OUT/install-v224.txt"
          adb shell pm grant com.suhas.multyfiautobuy.stable android.permission.POST_NOTIFICATIONS
          adb logcat -c
          for n in 1 2; do
            adb shell am force-stop com.suhas.multyfiautobuy.stable
            sleep 2
            adb shell am start -W -n com.suhas.multyfiautobuy.stable/.MainActivity | tee "$OUT/cold-launch-v224-$n.txt"
            sleep 10
            adb shell pidof com.suhas.multyfiautobuy.stable | tee "$OUT/pid-v224-$n.txt"
            test -s "$OUT/pid-v224-$n.txt"
          done
          adb shell uiautomator dump /sdcard/multyfi-v224-ui.xml
          adb pull /sdcard/multyfi-v224-ui.xml "$OUT/ui-v224.xml"
          grep -q 'Multyfi AutoBuy Pro' "$OUT/ui-v224.xml"
          grep -q 'stable release 2.2.4' "$OUT/ui-v224.xml"
          grep -q 'TRADE-TYPE BUDGETS' "$OUT/ui-v224.xml"
          grep -q 'Free recommendations use the saved Free budget' "$OUT/ui-v224.xml"
          ! grep -q '10 shares' "$OUT/ui-v224.xml"
          adb exec-out screencap -p > "$OUT/startup-v224.png"
          adb logcat -d -v threadtime > "$OUT/logcat-v224.txt"
          if grep -E 'FATAL EXCEPTION|Process: com.suhas.multyfiautobuy.stable|ClassNotFoundException.*com.suhas.multyfiautobuy.stable' "$OUT/logcat-v224.txt"; then
            exit 1
          fi

      - name: Create validation manifest
        shell: bash
        run: |
          set -euo pipefail
          OUT=android-stable/app/build/outputs/apk/debug
          SHA=$(sha256sum "$OUT/Multyfi-AutoBuy-Pro-S24-Android16-v2.2.4-CANDIDATE.apk" | awk '{print $1}')
          cat > "$OUT/VALIDATION-v2.2.4.json" <<EOF
          {
            "version": "2.2.4-candidate",
            "package": "com.suhas.multyfiautobuy.stable",
            "android_api": 36,
            "apk_sha256": "$SHA",
            "unit_tests": "PASS",
            "lint": "PASS",
            "signature": "PASS",
            "android16_install": "PASS",
            "cold_launch_1": "PASS",
            "cold_launch_2": "PASS",
            "crash_scan": "PASS",
            "budget_policy": {
              "Intraday": "saved Intraday amount",
              "Swing": "saved Swing amount; also ordinary/unlabelled equity",
              "Multibagger": "saved Multibagger amount",
              "Free": "saved Free amount; overrides other amount categories"
            },
            "execution_policy": "time remains only the weekday/market-hours gate and cancellation cutoff",
            "retained": {
              "routing": "explicit Intraday/MIS => MIS LIMIT; otherwise CNC entry GTT",
              "MIS_protection": "regular DAY SL_M SELL with product MIS",
              "CNC_protection": "broker-side stop-loss GTT",
              "daily_groww_verification": "automatic",
              "price_grid": "INR 0.10 generated-order grid"
            }
          }
          EOF

      - name: Upload v2.2.4 candidate and diagnostics
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: Multyfi-AutoBuy-Pro-S24-Android16-v2.2.4-CANDIDATE
          path: |
            apply-v224.log
            apply-v224-exit-code.txt
            build-v224.log
            build-v224-exit-code.txt
            android-stable/app/build/outputs/apk/debug/Multyfi-AutoBuy-Pro-S24-Android16-v2.2.4-CANDIDATE.apk
            android-stable/app/build/outputs/apk/debug/VALIDATION-v2.2.4.json
            android-stable/app/build/outputs/apk/debug/install-v224.txt
            android-stable/app/build/outputs/apk/debug/cold-launch-v224-*.txt
            android-stable/app/build/outputs/apk/debug/pid-v224-*.txt
            android-stable/app/build/outputs/apk/debug/ui-v224.xml
            android-stable/app/build/outputs/apk/debug/logcat-v224.txt
            android-stable/app/build/outputs/apk/debug/startup-v224.png
            android-stable/app/build/outputs/apk/debug/emulator-v224.log
            android-stable/app/build/reports/tests/testDebugUnitTest/
            android-stable/app/build/reports/lint-results-debug.html
          if-no-files-found: warn
