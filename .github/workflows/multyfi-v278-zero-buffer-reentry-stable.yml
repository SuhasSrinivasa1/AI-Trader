name: Multyfi v2.7.8 Zero Buffer + Reentry Stable

on:
  workflow_dispatch:

jobs:
  default-branch-stub:
    runs-on: ubuntu-latest
    steps:
      - run: echo 'Dispatch this workflow with ref multyfi-v2.7.4-final-stable to use the full v2.7.8 build definition from that branch.'
