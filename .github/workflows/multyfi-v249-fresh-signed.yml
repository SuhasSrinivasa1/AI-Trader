name: Multyfi v2.4.9 FRESH SIGNED APK

on:
  push:
    branches: [multyfi-v2.4.9-fresh-signed]
  workflow_dispatch:

jobs:
  build-sign-verify:
    runs-on: ubuntu-latest
    timeout-minutes: 40
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '17'
      - uses: android-actions/setup-android@v3
      - name: Install Android SDK
        run: yes | sdkmanager "platforms;android-36" "build-tools;35.0.0" "platform-tools"
      - uses: gradle/actions/setup-gradle@v4
        with:
          gradle-version: '8.13'

      - name: Apply validated v2.4.9 plus fresh package identity
        shell: bash
        run: |
          set -euo pipefail
          python3 hotfix/run_v249_fresh_signed.py | tee apply-v249-fresh.log

      - name: Build and test
        shell: bash
        run: |
          set -euo pipefail
          gradle --no-daemon --console=plain -p android-stable \
            :app:testDebugUnitTest :app:lintDebug :app:assembleRelease \
            | tee build-v249-fresh.log

      - name: Verify fresh package contracts
        shell: bash
        run: |
          set -euo pipefail
          grep -q "applicationId 'com.suhas.multyfiautobuy.v249fresh'" android-stable/app/build.gradle
          grep -q "versionCode 249" android-stable/app/build.gradle
          grep -q "versionName '2.4.9'" android-stable/app/build.gradle
          grep -q 'com.suhas.multyfiautobuy.stable.MainActivity' android-stable/app/src/main/AndroidManifest.xml
          grep -q 'com.suhas.multyfiautobuy.stable.MultyfiNotificationService' android-stable/app/src/main/AndroidManifest.xml
          grep -q 'Multyfi AutoBuy 2.4.9' android-stable/app/src/main/res/values/strings.xml
          M=android-stable/app/src/main/java/com/suhas/multyfiautobuy/stable
          S="$M/ProductionNotificationService.java"
          MON="$M/StrategyMonitorService.java"
          grep -q 'GrowwClient.placeEarlyExitMarketSell' "$S"
          grep -q 'Groww MARKET SELL was called before audit logging.' "$S"
          grep -q 'IntradayProtectionPolicy.multyfiStopHit' "$MON"
          grep -q 'IntradayProtectionPolicy.targetHit' "$MON"
          grep -q 'NotificationListenerHealth.ensureBound(this)' "$MON"
          grep -q 'STALE EARLY EXIT CLEARED — GROWW FLAT' "$S"

      - name: Generate one-time fresh signing key and sign APK
        shell: bash
        run: |
          set -euo pipefail
          KEYSTORE="$RUNNER_TEMP/multyfi-v249-fresh.jks"
          PASS='MultyfiV249Fresh_2026!'
          keytool -genkeypair -v \
            -keystore "$KEYSTORE" \
            -storepass "$PASS" \
            -keypass "$PASS" \
            -alias multyfi-v249-fresh \
            -keyalg RSA -keysize 4096 -validity 3650 \
            -dname 'CN=Multyfi AutoBuy v2.4.9 Fresh, OU=Private Build, O=Multyfi AutoBuy, L=Bengaluru, ST=Karnataka, C=IN'

          UNSIGNED=$(find android-stable/app/build/outputs/apk/release -maxdepth 1 -type f -name '*unsigned*.apk' | head -n1)
          test -n "$UNSIGNED"
          OUTDIR=android-stable/v249-fresh-signed
          mkdir -p "$OUTDIR"
          ALIGNED="$OUTDIR/Multyfi-AutoBuy-v2.4.9-FRESH-ALIGNED.apk"
          FINAL="$OUTDIR/Multyfi-AutoBuy-v2.4.9-FRESH-SIGNED.apk"
          "$ANDROID_HOME/build-tools/35.0.0/zipalign" -f -p 4 "$UNSIGNED" "$ALIGNED"
          "$ANDROID_HOME/build-tools/35.0.0/apksigner" sign \
            --ks "$KEYSTORE" \
            --ks-key-alias multyfi-v249-fresh \
            --ks-pass "pass:$PASS" \
            --key-pass "pass:$PASS" \
            --v1-signing-enabled true \
            --v2-signing-enabled true \
            --v3-signing-enabled true \
            --out "$FINAL" "$ALIGNED"
          rm -f "$ALIGNED" "$KEYSTORE"

      - name: Verify signed APK
        shell: bash
        run: |
          set -euo pipefail
          FINAL=android-stable/v249-fresh-signed/Multyfi-AutoBuy-v2.4.9-FRESH-SIGNED.apk
          "$ANDROID_HOME/build-tools/35.0.0/apksigner" verify --verbose --print-certs "$FINAL" | tee android-stable/v249-fresh-signed/SIGNATURE-VERIFY.txt
          "$ANDROID_HOME/build-tools/35.0.0/aapt" dump badging "$FINAL" | tee android-stable/v249-fresh-signed/BADGING.txt
          grep -q "package: name='com.suhas.multyfiautobuy.v249fresh'" android-stable/v249-fresh-signed/BADGING.txt
          grep -q "versionCode='249'" android-stable/v249-fresh-signed/BADGING.txt
          grep -q "versionName='2.4.9'" android-stable/v249-fresh-signed/BADGING.txt
          grep -q "application-label:'Multyfi AutoBuy 2.4.9'" android-stable/v249-fresh-signed/BADGING.txt
          sha256sum "$FINAL" | tee android-stable/v249-fresh-signed/SHA256.txt

      - name: Verify unit tests
        shell: bash
        run: |
          set -euo pipefail
          python3 - <<'PY'
          from pathlib import Path
          import xml.etree.ElementTree as ET
          root=Path('android-stable/app/build/test-results/testDebugUnitTest')
          tests=failures=errors=0
          for p in root.glob('TEST-*.xml'):
              x=ET.parse(p).getroot()
              tests += int(x.attrib.get('tests',0))
              failures += int(x.attrib.get('failures',0))
              errors += int(x.attrib.get('errors',0))
          print('v2.4.9 fresh signed', tests, failures, errors)
          assert tests >= 87
          assert failures == 0 and errors == 0
          PY

      - name: Upload signed APK only
        uses: actions/upload-artifact@v4
        with:
          name: Multyfi-AutoBuy-v2.4.9-FRESH-SIGNED
          path: android-stable/v249-fresh-signed/Multyfi-AutoBuy-v2.4.9-FRESH-SIGNED.apk
          if-no-files-found: error
